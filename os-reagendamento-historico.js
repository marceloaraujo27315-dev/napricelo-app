window.osReagHistPorAgenda=new Map();
function osReagSeparar(texto){const linhas=String(texto||'').split(/\n+/),hist=linhas.filter(l=>l.trim().startsWith('[REAGENDAMENTO ')),obs=linhas.filter(l=>!l.trim().startsWith('[REAGENDAMENTO ')).join('\n').trim();return {hist,obs};}
async function recarregarHistoricosReagendamentoOS(){try{const r=await fetch(`${SUPABASE_URL}/rest/v1/ordens_servico?select=id,agenda_id,numero,observacoes,checklist&order=id.desc`,{headers:SUPABASE_HEADERS});if(!r.ok)throw new Error(await r.text());const lista=await r.json(),map=new Map();for(const os of lista){const sep=osReagSeparar(os.observacoes),check=(os.checklist&&typeof os.checklist==='object')?{...os.checklist}:{},ja=Array.isArray(check.reagendamentos)?check.reagendamentos:[],hist=[...ja,...sep.hist].filter((v,i,a)=>a.indexOf(v)===i);if(hist.length&&os.agenda_id)map.set(Number(os.agenda_id),{osId:os.id,numero:os.numero,hist});if(sep.hist.length){check.reagendamentos=hist;try{await fetch(`${SUPABASE_URL}/rest/v1/ordens_servico?id=eq.${Number(os.id)}`,{method:'PATCH',headers:{...SUPABASE_HEADERS,Prefer:'return=minimal'},body:JSON.stringify({observacoes:sep.obs||null,checklist:check})});}catch(e){console.warn('Falha ao migrar histórico da OS',os.id,e);}}}window.osReagHistPorAgenda=map;decorarHistoricoReagendamentoOS();}catch(e){console.warn('Não foi possível carregar histórico de reagendamentos',e);}}
function osReagEsc(v){return String(v||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
function osReagAgendaIdDoTextarea(t){const form=t.closest('form');const ds=Number(form?.dataset?.agendaComercialId||0);if(ds)return ds;const g=Number(window.__osAgendaAtual||0);if(g)return g;const modal=t.closest('.modal,.detail-modal,.sheet,.drawer,[role=dialog]');if(modal){const txt=modal.textContent||'',m=txt.match(/OS-(\d{1,6})/);if(m){const n=Number(m[1]);for(const [agenda,d] of window.osReagHistPorAgenda.entries()){if(String(d.numero||'')===`OS-${String(n).padStart(5,'0')}`)return agenda;}}}return 0;}
function osReagEhObservacaoExecucao(t){const p=t.parentElement,prev=t.previousElementSibling,txt=`${p?.textContent||''} ${prev?.textContent||''}`.toLowerCase();return txt.includes('observações da execução')||txt.includes('observacoes da execucao');}
function decorarHistoricoReagendamentoOS(){document.querySelectorAll('textarea').forEach(t=>{if(t.dataset.reagHistDecorado==='1'||!osReagEhObservacaoExecucao(t))return;const agendaId=osReagAgendaIdDoTextarea(t),dados=window.osReagHistPorAgenda.get(Number(agendaId));if(!dados?.hist?.length)return;t.dataset.reagHistDecorado='1';const box=document.createElement('div');box.className='os-reag-historico';box.innerHTML=`<div class="os-reag-head"><b>Histórico de reagendamentos</b><span>Registro automático • somente leitura</span></div>${dados.hist.map((h,i)=>`<div class="os-reag-item"><small>Reagendamento ${i+1}</small><div>${osReagEsc(h.replace(/^\[REAGENDAMENTO\s*/,'').replace(/\]\s*/, ' — '))}</div></div>`).join('')}`;const alvo=t.parentElement||t;t.parentNode.insertBefore(box,alvo);});}
(function instalarHistoricoReagendamentoOS(){const st=document.createElement('style');st.textContent='.os-reag-historico{margin:14px 0;padding:12px;border:1px solid #cfe0d7;border-left:4px solid #176b45;border-radius:10px;background:#f7fbf9}.os-reag-head{display:flex;justify-content:space-between;gap:10px;align-items:baseline;margin-bottom:8px;color:#176b45}.os-reag-head span{font-size:11px;color:#68786f}.os-reag-item{background:#fff;border:1px solid #e0e8e3;border-radius:8px;padding:9px 10px;margin-top:7px;color:#30483b}.os-reag-item small{display:block;color:#176b45;font-weight:700;margin-bottom:3px}@media(max-width:650px){.os-reag-head{display:block}.os-reag-head span{display:block;margin-top:2px}}';document.head.appendChild(st);const obs=new MutationObserver(()=>decorarHistoricoReagendamentoOS());obs.observe(document.documentElement,{childList:true,subtree:true});setTimeout(recarregarHistoricosReagendamentoOS,900);window.recarregarHistoricosReagendamentoOS=recarregarHistoricosReagendamentoOS;})();